version: 1
metadata:
  some_key: $HOME/ff97e4c6
steps:
  - name: meta
    metadata:
      step_key: $HOME/hello
    command: "{{ index .MergedMetadata \"step_key\" }}/middle/{{ index .MergedMetadata \"some_key\"}}"
